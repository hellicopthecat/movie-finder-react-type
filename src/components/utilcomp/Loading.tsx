import styled from "styled-components";

const LoadingCont = styled.div``;
const Loading = () => {
  return (
    <LoadingCont>
      <h1>Loading</h1>
    </LoadingCont>
  );
};
export default Loading;
